# ملاحظات المشروع — الديوانية · الجزء ٢ (المرجع الجاري)

> الملف الأول `NOTES.md` امتلأ — هذا الملف هو المرجع من الآن. يكمّل عليه.

## الحالة الحالية (منشور لايف)
- فرع التطوير: `claude/keen-noether-m3vjnl` · النشر من `main` تلقائياً على Vercel.
- الرابط: `https://my-project-git-main-ak-s-pr.vercel.app`
- **المستشار الشخصي (A1):** الرئيسية الجديدة — أقسام يمين + «سؤال اليوم» يبني ملفاً خاصاً (localStorage، غير قابل للنشر) → أنماط + نصائح عبر `/api/profile-insights` (+ fallback محلي + حارس مواضيع حسّاسة).
- **الفريج (M1 + النقاش الحيّ):** أداة من زر «أدوات». مشهد SVG، 24 شخصية، **فيسات تتغيّر + نقاش ٤ جولات + عدوى مزاجية** (العصب يعدّي، الطيف السلبي يصعّد) + **الحكم يتأثّر بالنقاش (دراما عالية، يقلب 🟢↔🔴)**. عقد البيانات: `react(result, onComplete)` يرجّع حكماً متأثّراً بالنقاش، والنتيجة تحت تتحدّث.

## معلّق على المالك
1. 🔴 **«المفتاح»** (ANTHROPIC_API_KEY بـVercel + Redeploy) — يفعّل Claude الحقيقي بدل المحاكي. (قُل «المفتاح» للخطوات الكاملة.)
2. 🟡 **تعطيل Vercel Deployment Protection** لو طلب تسجيل دخول للزوّار.

## 🗑️ تدقيق الملفات الزايدة (بانتظار قرار المالك — لا تُحذف قبل الموافقة)
- `llm-bridge.js` (5KB) — **ميت**، استبدله `api/generate.js`، غير محمّل بأي صفحة. → يُحذف.
- `docs/gap-report.md` (7KB) — **متجاوَز** بـNOTES. → يُحذف.
- `app.html` (11KB) — نسخة مبسّطة يتيمة، ما فيها فريج/مستشار، بلا رابط. → قرار المالك.
- `docs/partner-deck.pptx` (288KB) — **أثقل ملف** بالمشروع. → قرار المالك.
- يبقى: `engine-spec-v4.6.md` (مرجع)، `NOTES.md`+`NOTES-2.md`، كل الكود الحيّ.

## 🧠 المهمة الجاية: «تحليل النقاش» (طلب المالك)
بعد النقاش، تقرير يوضّح: **منو تواجه مع منو** · لكل شخصية **بدأ→انتهى بأي مزاج** · **منو غيّر رأيه** · **منو أثّر عليه وليش** · استنتاجات عامة. يُبنى من نفس نموذج العدوى (نسجّل «منو سحب منو» بكل جولة — provenance). القرارات النهائية بعد أسئلة التوضيح.

## خط ثابت
«نصوّر النار، ما نشعلها» — واقعية بلا سقف بالتمثيل، بلا إنتاج تحريض/تشهير حقيقي. رأي قانوني بشري قبل أي إطلاق عام (خارج نطاقي).
