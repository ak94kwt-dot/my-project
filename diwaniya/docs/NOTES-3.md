# ملاحظات المشروع — الديوانية · الجزء ٣ (المرجع الجاري النشط)

> `NOTES.md` و`NOTES-2.md` **مُغلقان** (امتلآ). هذا الملف هو المرجع النشط من الآن. يكمّل عليه.

## الحالة الحالية (كلّه منشور لايف على Vercel من `main`)
- الرابط: `https://my-project-git-main-ak-s-pr.vercel.app`
- فرع التطوير: `claude/keen-noether-m3vjnl` (متزامن مع `main`). أي push على `main` ينشر تلقائياً.
- Vercel: team `team_BdMPEvXh1WlN3Ex1tGMrOWII` (slug `ak-s-pr`) · project `prj_Ph0tvPg0oU6jwDitmAfYBxMbNm86`.
- ٣ دوال سيرفرليس شغّالة (`nodejs:3`).

## وش بُني وصار لايف
1. **المستشار الشخصي (الرئيسية):** أقسام يمين + «سؤال اليوم» خفيف → ملف خاص محلي (localStorage) **غير قابل للنشر** → أنماط شخصية + نصائح عبر `/api/profile-insights` (+ fallback محلي + حارس مواضيع حسّاسة: دين/سياسة/صحة عميقة لا تُرسل للسيرفر).
2. **الفريج الحيّ (أداة من زر «أدوات»):** مشهد SVG، 24 شخصية، مساحة كبيرة.
   - **فيسات تتغيّر** (😄🙂😐😠😡) بدل فقاعات الكلام.
   - **نقاش ٤ جولات + عدوى مزاجية:** العصب يعدّي أسرع، الطيف السلبي (ترول/متصيّد) يصعّد جيرانه.
   - **الحكم يتأثّر بالنقاش (دراما عالية):** يقدر يقلب 🟢↔🔴. يعرض «بدأ X٪ ← بعد النقاش Y٪ · السبب».
   - **لوحة تحليل النقاش** (تحت المشهد): منو تواجه منو · لكل شخصية بدأ←انتهى ومنو أثّر عليه وليش · استنتاجات عامة. محلية + سرد AI عبر `/api/debate-narrate` لو فيه «المفتاح».

## الملفات الحيّة
- `index.html` · `app.js` (الموقع + المستشار + لوحة النتائج/التحليل)
- `engine.js` (محرّك قواعدي fallback) · `questions.js` · `profile.js` · `advisor.css`
- `freej.js` · `freej.css` (المشهد + العدوى + التحليل)
- `api/generate.js` · `api/profile-insights.js` · `api/debate-narrate.js`
- `docs/engine-spec-v4.6.md` (مرجع) · `docs/partner-deck.pptx` (عرض شركاء) · `NOTES*.md`
- (انحذف للتنظيف: `llm-bridge.js`, `app.html`, `docs/gap-report.md`)

## معلّق على المالك (لا يُنفّذ بدون طلبه)
1. 🔴 **«المفتاح»** (ANTHROPIC_API_KEY بـVercel + Redeploy) — يفعّل Claude الحقيقي بدل المحاكي، ويفعّل سرد AI للتحليل. (قُل «المفتاح» للخطوات الكاملة.)
2. 🟡 **تعطيل Vercel Deployment Protection** لو طلب تسجيل دخول للزوّار.
3. 🔵 **رأي قانوني (محامٍ بشري)** قبل أي إطلاق عام للملف الخاص — خارج نطاقي، لا أقدّم استشارة قانونية.

## أفكار للتطوير الجاي (Backlog)
- 🟢 شخصيات SVG مرسومة كاملة بدل الإيموجي (M2 للفريج).
- 🟢 إثراء المجتمعات بالمستشار (تفاعل فعلي) + نصائح يومية استباقية + تذكيرات/streak.
- 🟡 معايرة دقّة المحرّك · نطاق أنظف · تحسينات واجهة.
- 🟢 A3: Supabase + تسجيل دخول + ملف دائم عبر الأجهزة (بعد محامٍ).

## خط ثابت
«نصوّر النار، ما نشعلها» — واقعية بلا سقف بالتمثيل، بلا إنتاج تحريض/تشهير حقيقي.
